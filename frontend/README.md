# Frontend - Book Quote Shorts

## Environment
Create `.env` in `frontend/` (optional):

```
VITE_API_BASE=http://localhost:4000/api
```

## Run
```
npm install
npm run dev
```

Open `http://localhost:5173`.
